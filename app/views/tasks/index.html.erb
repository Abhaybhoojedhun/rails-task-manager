<% @tasks.each do |task| %>
  <p>
    <%= link_to task.title, task_path(task) %> - <%= link_to "Delete",
    task_path(task),
    data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    %>
  </p>
<% end %>

<p><%= link_to "add a new task", new_task_path %></p>

<%# check_box_tag(link_to task.title, task_path(task))  %>

<%# <%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>
